# Mosquitto 2 defaults to localhost-only with anonymous access disabled.
# The firmware connects with a client id but no credentials, so allow
# anonymous connections on the LAN-facing listener. No auth — keep this
# broker on the trusted local network only.
listener 1883
allow_anonymous true

# Survive container restarts so queued/retained state isn't lost.
persistence true
persistence_location /mosquitto/data/
